[package]
name = "stadhouder"
version = "0.1.0"
edition = "2021"

[lib]
name = "stadhouder"
path = "src/lib.rs"

[dependencies]
common = { path = "../common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
